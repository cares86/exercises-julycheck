var d = new Date();
var md = d.getMonth();
month(md);

function month(d){



if (d =='6') {
   alert("It's July, so it can get really hot.");
}
else {
    alert("It’s not July, so at least we’re not in the peak of the heat.");
}
}
